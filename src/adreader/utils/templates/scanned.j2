{% macro coords(c) -%}
  [[{{ "%.0f"|format(c[0][0]) }},{{ "%.0f"|format(c[0][1]) }}], [{{ "%.0f"|format(c[1][0]) }},{{ "%.0f"|format(c[1][1]) }}], [{{ "%.0f"|format(c[2][0]) }},{{ "%.0f"|format(c[2][1]) }}], [{{ "%.0f"|format(c[3][0]) }},{{ "%.0f"|format(c[3][1]) }}]]
{%- endmacro %}
{
{%- for pnum, page in pages.items() -%}
{% include "page.j2" with context %}{% if not loop.last %},{% endif %}
{% endfor -%}
}
